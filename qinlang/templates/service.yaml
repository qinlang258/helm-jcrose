apiVersion: v1
kind: Service
metadata:
  name: {{ include "qinlang.fullname" . }}
  labels:
    {{- include "qinlang.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qinlang.selectorLabels" . | nindent 4 }}
