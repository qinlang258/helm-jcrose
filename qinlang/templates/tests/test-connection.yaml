apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qinlang.fullname" . }}-test-connection"
  labels:
    {{- include "qinlang.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qinlang.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
