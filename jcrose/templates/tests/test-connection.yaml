apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jcrose.fullname" . }}-test-connection"
  labels:
    {{- include "jcrose.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jcrose.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
